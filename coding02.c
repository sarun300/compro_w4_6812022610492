#include <stdio.h>      // รูปแบบที่ถูกต้องของ include

int main() {            // main function เมื่อจบคำสั่งควรใส่ ; ทุกครั้ง
    int a = 10;         // declare variable เมื่อจบคำสั่งควรใส่ ; ทุกครั้ง
    printf("a = %d", a); // output เมื่อจบคำสั่งควรใส่ ; ทุกครั้ง
    return 0;           // เมื่อจบคำสั่งควรใส่ ; ทุกครั้ง
}
